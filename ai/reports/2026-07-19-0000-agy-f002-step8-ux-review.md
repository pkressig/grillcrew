# AGY UX Review - F002 Step 8 Frontend Auth Shell

- Agent: AGY / Antigravity
- Timestamp: 2026-07-19T00:00:00+02:00
- Branch: feature/f002-step8-frontend-auth-shell
- Assignment: UX, accessibility and product-flow review of F002 Step 8 frontend authentication shell

## Verdict

Ready to commit. No blocking UX or accessibility issues were found.

## Strengths

- Mobile-first touch targets use safe sizing such as 44px-equivalent controls.
- Admin shell handles loading, unauthenticated and unauthorized states clearly.
- Login flow adapts to dynamic organization branding from tenant context.
- Invitation preview behavior is privacy-safe and exposes only public-safe organization information.
- Validation is green: backend tests, frontend tests, linting, formatting and production build pass.

## Issues

Medium:

- `organization-switcher.tsx` does not visually distinguish the current organization from other membership links.

Low:

- Existing active users are still asked for `Anzeigename` during invitation acceptance, even though the backend ignores that value for already-active users.

## Copy Suggestions

- Prefer real German umlauts in user-facing UI copy instead of digraphs such as `ungueltig`, `moeglich`, `laeuft`, `oeffnen`, `geaendert`, `zuruecksetzen`.
- Translate technical role enum values into human-readable labels such as `Administration`, `Koordination`, `Kiosk`, and `Vorstand (Lesen)`.

## Follow-Up Recommendation

These items are small enough to fix before commit if the implementation is low-risk. Otherwise they can be tracked as UI polish backlog items.
